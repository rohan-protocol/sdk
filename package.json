{
  "name": "rohan-protocol",
  "version": "0.5.0",
  "private": true,
  "description": "Rohan Protocol – Zero-Knowledge Agent Handshake Monorepo",
  "scripts": {
    "build": "npm run build --prefix packages/sdk && npm run build --prefix packages/webmcp && npm run build --prefix packages/mcp",
    "clean": "npm run clean --prefix packages/sdk && npm run clean --prefix packages/webmcp"
  }
}
